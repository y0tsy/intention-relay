# Third-Party Notices

> This file is automatically generated. Do not edit it directly.
>
> Inputs: `Cargo.lock`, `quality/about.toml`, and
> `quality/third_party_notices.hbs`. Regenerate with `make notices` and verify
> freshness with `make notices-check`.

This document lists license information harvested for registry dependencies in
the locked Intention Relay workspace dependency graph. Local workspace packages
are excluded because they are project-owned code; their license is declared in
the root `Cargo.toml`.

## License index

{{#each overview}}
- [{{name}}](#{{id}}), used by {{count}} dependency entries
{{/each}}

## Full license texts

{{#each licenses}}
{{#if first_of_kind}}
### {{name}}
{{/if}}

#### Used by

{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```text
{{{text}}}
```

{{/each}}
